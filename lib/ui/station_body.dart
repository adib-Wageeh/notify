import 'package:flutter/material.dart';

class StationBody extends StatelessWidget {
  const StationBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Center(child: Text("station")));
  }
}
