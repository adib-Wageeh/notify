import 'package:flutter/material.dart';

class CreateBody extends StatelessWidget {
  const CreateBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Center(child: Text("create")));
  }
}
