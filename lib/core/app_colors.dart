import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Used for the brightest green tint: selected chips and gentle highlights.
  static const primary100 = Color(0xFFD6EFE0);

  // Used for light green surfaces: icons behind reminder types.
  static const primary200 = Color(0xFFBDE6CD);

  // Used for emphasis on dark surfaces and the main primary button.
  static const primary400 = Color(0xFF82E2A6);

  // Used for active navigation text and success/connected states.
  static const primary500 = Color(0xFF5CCB86);

  // Used for secondary green text and links on light surfaces.
  static const primary600 = Color(0xFF3EAD69);

  // Used for strong green elements: active tabs and light-mode buttons.
  static const primary700 = Color(0xFF286B45);

  // Used for dark green cards, such as the “Run notification test” panel.
  static const primary800 = Color(0xFF23402F);

  // Used for the darkest green system-bar surface.
  static const primary900 = Color(0xFF17201D);

  // Used for the app’s lightest background in light mode.
  static const grey50 = Color(0xFFF8FBF8);

  // Used for subtle light surfaces and disabled backgrounds.
  static const grey100 = Color(0xFFF0F5F1);

  // Used for dividers, card borders, and subtle separators.
  static const grey200 = Color(0xFFE0E9E2);

  // Used for light-mode secondary text and inactive icons.
  static const grey300 = Color(0xFFC1CDC3);

  // Used for dark-mode secondary text and inactive bottom-navigation items.
  static const grey400 = Color(0xFFADBBB1);

  // Used for muted text, timestamps, and less-important information.
  static const grey500 = Color(0xFF738077);

  // Used for secondary text on light backgrounds.
  static const grey600 = Color(0xFF526B5B);

  // Used for dark text panels and outlined input backgrounds.
  static const grey700 = Color(0xFF293F32);

  // Used for the main dark app background.
  static const grey800 = Color(0xFF1E382B);

  // Used for the dark bottom navigation and deepest app surfaces.
  static const grey900 = Color(0xFF1B2A22);

  // Used for primary button text on the bright green primary400 button.
  static const ink900 = Color(0xFF11261A);

  // Used for primary text on dark backgrounds.
  static const white = Color(0xFFFFFFFF);
}